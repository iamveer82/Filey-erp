# Filey worker config. Copy to .env (local) or set as deploy secrets.
# Service-role key — KEEP SECRET, server-only. Never ship to the client.
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
POLL_INTERVAL_MS=5000
