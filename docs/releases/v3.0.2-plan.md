# Filey ERP 3.0.2 release

1. Commit the audited pending changes with version 3.0.2 in npm and Tauri/Cargo metadata. Keep secrets and generated test output out of Git.
2. Require CI and the Vercel preview to pass on the patch PR before merging.
3. Tag the merged commit `v3.0.2`; the existing tag workflow builds a new draft. Do not reuse older tags or drafts, or restart the stopped automation.
4. Require Windows NSIS/MSI, Linux DEB/RPM and both macOS architectures. Verify manifest version/tag URLs, installer sizes, signature references and cryptographic updater signatures using the unchanged public key.
5. Publish the verified draft as latest using `v3.0.2.md`. Fetch the public updater endpoint to confirm version 3.0.2 and check production web deployment separately.

The 23 September read-only live schema audit passed (84 tables, 1,115 columns, 56 functions, 45 sync collections). No new schema migration is required for the workspace-switch fixes. Existing customer records and conflict choices must remain unchanged. Do not make paid transactions or enable unconfigured providers for release testing.
