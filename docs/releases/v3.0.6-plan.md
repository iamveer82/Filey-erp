# Filey ERP 3.0.6 release

1. Ship workspace-wide device/cloud reconciliation, owner-verified recovery of retired workspaces, private backup of nullable legacy invoice rows, and atomic desktop conflict-choice writes. Preserve records found on only one side and retain revision checks for edits made during sync. Automatic sync remains opt-in; this release does not add instant live streaming.
2. Include invoice-specific AI logo/stamp/signature controls, 100% opacity defaults for new marks, and shared document mark controls. Preserve existing custom opacity and do not modify customer documents while validating.
3. The schema-only migration `supabase/2026-09-25-workspace-sync-recovery.sql` was applied and read back in production. No customer recovery RPC was executed. Require the disposable PostgreSQL recovery/security tests, full CI, and Vercel preview on the final PR commit.
4. Merge the checked PR and tag its merged commit `v3.0.6`. Run the existing release workflow and retain previous tags, releases and the stopped automation.
5. Require Windows EXE/MSI, Linux DEB/RPM, and both Mac architectures. Download draft assets and verify versions, SHA256 digests, updater URLs and cryptographic signatures against the unchanged updater key; inspect Mac app and WhatsApp sidecar architectures.
6. Add the release notes to `latest.json`, publish the verified draft as latest, and verify the public updater/downloads. Check the production web deployment separately at the merged commit. Do not send real WhatsApp messages, make payments, or run sync against customer records as a release test.
