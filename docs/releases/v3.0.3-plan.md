# Filey ERP 3.0.3 release

1. Keep npm, Tauri and Cargo versions at 3.0.3. Review and commit the authorized pending changes, excluding credentials, generated binaries and test output.
2. Finish desktop connection and UI checks before release. Saved WhatsApp pairing reconnection passed in the rebuilt desktop app after fixing launcher environment propagation. Do not claim live owner-message or PDF delivery is verified without an observed result. Use disposable records/profiles for tests; preserve customer data and pairing unless the user explicitly requests a reset.
3. Require CI and Vercel preview checks on the final PR commit. CI covers typecheck, lint, app tests, disposable RLS, browser tools, bridge media/dependency checks, backend tests and production bundles. The release workflow also runs native storage/security tests on every target.
4. Merge the passing PR, tag its merged commit `v3.0.3` and use the existing release workflow to create a new draft. Keep prior tags and drafts unchanged and the stopped release automation off.
5. Require Windows EXE/MSI, Linux DEB/RPM and both macOS architectures. Verify installer versions, sizes and hashes; verify `latest.json` version, v3.0.3 URLs, signature references and cryptographic updater signatures against the unchanged public key. The workflow's asset-presence check does not replace signature verification.
6. Ship the native executable, WhatsApp sidecar and frontend together: replies now require native session IDs, and incoming events include their bridge session. An older native executable with the new frontend deliberately fails closed.
7. Publish the verified draft as latest using `v3.0.3-notes.md`. Fetch the public updater manifest and referenced downloads again, then check the production web deployment independently against the merged SHA. Verify startup and the updated integrations/chat UI without changing customer records.

## Validation status at preparation

The targeted WhatsApp, computer/browser, approval and formatting checks passed (68 tests); the final lifecycle suite passed all 20 tests after adding provider-error redaction. `tsc --noEmit` passed. Lint passed with 0 errors and 536 warnings; the release-recovery guard passed.

The first full app run completed with 1,758 passing and 3 failing tests across 269 files. All three failures were repaired: the browser's missing ResizeObserver test fixture passed its 6-test targeted rerun; delivery-challan schemas now preserve existing blank-line filtering, default quantities and unknown-type fallback while still rejecting invalid quantity types. The challan/schema/harness rerun passed all 52 tests. Both full CI runs and Vercel preview subsequently passed for commit 257f5f1.

The final startup repair passes the session directory/owner explicitly to the bridge; its parser, transport and identity checks passed 22 tests. The compiled bridge passed a QR smoke with an empty environment and the rebuilt desktop app showed Connected using the retained pairing. Desktop history collapse/Escape, browser pointer/keyboard resizing, collapse and embedded Instagram loading passed. Production frontend/native builds, browser-tool fixtures and bridge media/dependency audit passed. Require fresh CI for this final startup change, then installers and post-publication checks. No live WhatsApp reply or PDF-delivery result is claimed here.

Do not make paid transactions or turn on unconfigured wallet/media providers for release verification. Keep the previous published release available; fix release defects with a new forward patch rather than overwriting signed assets or rolling back customer data.
