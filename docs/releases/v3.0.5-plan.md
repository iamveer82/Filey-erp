# Filey ERP 3.0.5 release

1. Verify the expired-session fix with the reported Auth error, real Supabase client fixtures, database/file upload rejection, concurrent renewal, refresh failure, sign-out/account changes and preservation of pending local records. Do not run sync against customer records.
   Verify WhatsApp clock skew in both directions without replaying history, typing/acknowledgement cleanup, owner-only routing, fast greetings, queued tasks and file delivery. Build the updated bridge sidecar; retain existing pairing. Inspect History and Browser toolbar icons.
2. Require typecheck, lint, app tests and production build locally, plus the existing full CI checks and Vercel preview on the final PR commit. No backend or schema deployment is required.
3. Merge the checked PR and tag its merged commit v3.0.5. Use the existing release workflow; retain all previous tags/releases and leave the stopped automation unchanged.
4. Require Windows EXE/MSI, Linux DEB/RPM and both Mac architectures. Download the draft assets; verify their versions, SHA256 digests, updater URLs/signatures and the Mac app/WhatsApp sidecar architectures using the same verification as 3.0.4.
5. Add the reviewed release notes to latest.json, verify the final manifest, publish as latest, and confirm the public updater serves 3.0.5 with working installer URLs. Verify production Vercel separately at the merged commit.
