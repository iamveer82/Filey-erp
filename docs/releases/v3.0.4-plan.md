# Filey ERP 3.0.4 release

1. Keep npm, Tauri and Cargo versions at 3.0.4. Commit the reviewed Paper wallet, named OpenRouter selection, custom top-up and mascot changes; exclude credentials, fixtures, generated installers and logs.
2. Validate the agent and WhatsApp code with the full app and edge suites, bridge launch/media/security checks, native fixtures, lint, typecheck, RLS and production builds. Preserve customer records and pairing. Record live pairing/reply/PDF results separately; never claim phone delivery from mocks or a Connected label alone.
3. Deploy the reviewed `ai-credits` and `dodo` functions using existing authenticated access, preserving provider/payment configuration. The frontend hides custom amounts when the backend lacks their configuration. Do not activate OmniRoute or an unconfigured paid provider and do not make paid transactions.
4. Require CI and Vercel preview on the final PR commit, merge, and tag the merged SHA `v3.0.4`. Use the existing release workflow for a new draft. Leave old tags/drafts and the stopped automation unchanged.
5. Require Windows EXE/MSI, Linux DEB/RPM and both Mac architectures. Validate version, sizes, SHA256 digests, updater URL references, and all cryptographic updater signatures against the unchanged public key. Check native application and WhatsApp sidecar architectures.
6. Publish the verified draft as latest using `v3.0.4-notes.md`. Verify the public latest manifest and asset downloads, and check the separate production web deployment at the merged SHA. Report any unverified live provider/phone behavior explicitly.

The optional OmniRoute adapter remains inactive by default. Direct OpenRouter is the built-in route; secrets stay server-side. No database schema migration or customer-data rewrite is needed for this patch.
