# Filey ERP 3.0.1 release

1. Apply `supabase/2026-09-22-batched-sync.sql` after the existing conflict-protection migration. Applied on 22 September; production function body hash `d4ee83cc2755bbde1f4636c4059bbbcc` matches the tested source. SECURITY INVOKER, authenticated execution allowed, anonymous execution denied. No customer rows changed.
2. Dodo checkout handler and shared return helper deployed on 22 September through the signed-in dashboard. Source matched the tested files; anonymous status requests return 401. Keep unconfigured wallet providers disabled. No paid transaction was made.
3. Pass CI and preview deployment on the patch PR, then merge. Check the production web deployment separately.
4. Tag the merged commit `v3.0.1` and run the existing release workflow. Never modify `v3.0.0` or reuse an older draft.
5. Require Windows NSIS/MSI, Linux DEB/RPM and both macOS architectures. Check `latest.json` version, tag URLs, asset sizes and exact signature references; verify downloaded updater artifacts cryptographically with the unchanged Tauri public key.
6. Publish the verified draft as latest with `v3.0.1.md`, then fetch the public auto-updater endpoint and confirm it serves 3.0.1. Keep the existing release automation paused.

## Recovery limits

The live catalog matches the audited column names/types for all 17 reported failing collections. Local legacy data contains records from two companies and invoice children without parent IDs. Preserve them for explicit review; do not resolve their conflicts by guessing a company or invoice. New batching retains per-row RLS, revision and idempotency checks. Older servers use the single-row RPC fallback.

## Checks

The batch test sends 120 rows using three requests, isolates row failures, keeps edits made during requests, retains queued deletions and refuses cross-company uploads. Clean tables continue receiving cloud changes when another table needs review. Disposable PostgreSQL tests exercise RLS, stale revisions, identical retries, invalid rows between valid siblings, and idempotent migration application. The 1,670-test app suite, 85 edge tests, lint (existing warnings only) and production build passed; the additional partial-sync regression also passed. CI must validate the final commit.
