{
  "name": "filey-wa-bridge",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "QR-paired WhatsApp bridge for the Filey agent. Deliberately OUTSIDE the app's package.json — Baileys must never end up in the desktop bundle.",
  "bin": { "filey-wa-bridge": "index.mjs" },
  "scripts": { "start": "node index.mjs" },
  "dependencies": {
    "@whiskeysockets/baileys": "^6.7.9",
    "qrcode": "^1.5.4",
    "qrcode-terminal": "^0.12.0"
  }
}
